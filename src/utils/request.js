import axios from 'axios'


//创建axios实例
class HttpRequest {
  constructor(baseUrl = baesURL) {
    this.baseUrl = baseUrl,
      this.queue = {}
  }
  getInsideConfig() {
    const config = {
      baseUrl=this.baseUrl,
      headers:{
        
      }
    }
  }
}
